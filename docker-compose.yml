services:
  visiona-data-api:
    build:
      context: .
      dockerfile: dockerfile
    container_name: visiona-data-api
    ports:
      - "8080:8080"
      - "8443:8443"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - ./certs:/app/certs:ro
    networks:
      - visiona-network

  redis:
    image: redis:7.4-alpine
    container_name: visiona-data-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "no"]
    volumes:
      - redis_data:/data
    networks:
      - visiona-network
    restart: unless-stopped

volumes:
  redis_data:
    driver: local

networks:
  visiona-network:
    driver: bridge